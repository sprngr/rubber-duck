export const DUCK_USAGE = {
  policy: "Usage: /duck policy on|off",
  policyScope: "Usage: /duck policy-scope <subagents|session>",
  mode: "Usage: /duck mode on|off",
  debug: "Usage: /duck debug on|off",
  debugVerbose: "Usage: /duck debug-verbose on|off",
  debugStatus: "Usage: /duck debug-status",
  route: "Usage: /duck route <text>",
  routePreview: "Usage: /duck route preview <text>",
  followup: "Usage: /duck followup <text>",
  refine: "Usage: /duck refine [--replace] <text>",
  refineReplace: "Usage: /duck refine --replace <text>",
  reply: "Usage: /duck reply <requestId> <decision> [notes]",
  pauseRun: "Usage: /duck pause-run [runId]",
  resumePausedRun: "Usage: /duck resume-run <runId>",
} as const;

export const DUCK_HELP_LINES = [
  "Duck commands",
  "Legend: <> required · [] optional · a|b choices",
  "Ambient routing default: on (use /duck mode off for manual-only)",
  "Policy scope default: session (applies to main session + subagents when policy is on)",
  "",
  "Core",
  "- /duck status — show extension status, pending workflow, and follow-up state",
  "- /duck reset — reset duck state and clear pending contexts",
  "- /duck on | /duck off — enable or disable duck extension behavior",
  "- /duck policy <on|off> — enable or disable packaged AGENTS policy usage",
  "- /duck policy-scope <subagents|session> — choose policy target scope (default: session)",
  "- /duck mode <on|off> — toggle ambient routing for normal (non-slash) messages",
  "",
  "Routing workflow",
  "- /duck route <text> — preview route intent/skill/chain for input text",
  "- /duck route preview <text> — explicit route-preview variant",
  "- /duck proceed [summary] — start queued routed chain with optional summary",
  "- /duck refine [--replace] <text> — append/replace refinement notes before proceed",
  "- /duck cancel — discard queued workflow and return to idle",
  "",
  "Run control",
  "- /duck runs — list recent runs with runId, derived title, state, and chain",
  "- /duck pending — list pending supervisor requests requiring replies",
  "- /duck reply <requestId> <continue|stop|retry-later> [notes] — answer a supervisor request",
  "- /duck resume <runId> [step] — continue an existing run from stored cursor/step",
  "- /duck pause-run [runId] — stash current run/follow-up context for later",
  "- /duck resume-run <runId> — restore stashed run/follow-up context and continue",
  "",
  "Follow-up",
  "- /duck followup <text> — send explicit follow-up context to the active run",
  "- /duck close-run — close active follow-up context (new messages route normally)",
  "",
  "Debug",
  "- /duck debug <on|off> — toggle debug cards for route and chain I/O",
  "- /duck debug-verbose <on|off> — expand debug preview lengths",
  "- /duck debug-status — show effective debug settings and preview caps",
  "",
  "Direct ducklings",
  "- /duck-reviewer <task> — run reviewer duckling directly",
  "- /duck-investigator <task> — run investigator duckling directly",
  "- /duck-builder <task> — run builder duckling directly",
  "- /duck-adversary <task> — run adversary duckling directly",
  "- /duck-dry <task> — run dry duckling directly",
  "- /duck-simple <task> — run simple duckling directly",
  "",
  "Env",
  "- DUCK_AGENT_TIMEOUT_MS (default 120000) — max runtime per subagent invoke",
  "- DUCK_AGENT_FORCE_KILL_GRACE_MS (default 2000) — grace before force-kill",
] as const;
