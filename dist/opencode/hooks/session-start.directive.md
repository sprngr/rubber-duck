# 🦆 Session start directive

MANDATORY INSTRUCTION: Your first action in this session MUST be to invoke the
`duck-policy` skill via the skill tool with name "duck-policy" and load it. Do
not take any other action, tool call, or response until the skill is loaded.
Apply it for the entire session. This is a directive, not a suggestion.