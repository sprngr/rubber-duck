{
  "skill_name": "duck-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Please review this diff for checkout totals. Keep comments terse and paste-ready for PR:\n\n```diff\n- const total = subtotal + tax || 0\n+ const total = subtotal + tax\n+ if (discountCode) total -= discountAmount\n```\n",
      "expected_output": "Produces concise duck-review findings in one-line prefixed format with location, problem, and Fix field; identifies correctness risk in total calculation path and gives smallest safe fix direction.",
      "assertions": [
        "Uses one-line finding format with approved prefix token and includes location + problem + 'Fix:' on each finding.",
        "Flags the highest-risk correctness issue before lower-priority comments.",
        "Provides an actionable smallest-safe fix direction tied to the shown diff."
      ]
    },
    {
      "id": 2,
      "prompt": "Can you run a duck-review on this? I forgot to paste the diff, but we changed auth middleware yesterday.",
      "expected_output": "Asks one targeted clarifying question requesting the missing review artifact (diff/code/PR text) before giving findings; does not invent line-level comments without evidence.",
      "assertions": [
        "Requests the concrete review target when artifact is missing.",
        "Avoids fabricated findings with fake file paths or line numbers.",
        "Keeps the clarification concise and directly actionable."
      ]
    },
    {
      "id": 3,
      "prompt": "Review this mixed-risk diff and call out what matters first:\n\n```diff\n--- a/src/api/user.ts\n+++ b/src/api/user.ts\n@@ -40,7 +40,7 @@ export async function getUser(req, res) {\n- const q = `SELECT * FROM users WHERE id = ${req.query.id}`\n+ const q = `SELECT * FROM users WHERE id = ${req.query.id}`\n@@ -82,6 +82,7 @@ export async function getUser(req, res) {\n+ console.log('auth token', req.headers.authorization)\n@@ -120,7 +121,7 @@ export async function getUser(req, res) {\n- const timeoutMs = 3000\n+ const timeoutMs = 300000\n```\n",
      "expected_output": "Prioritizes risk-first findings (security before performance), uses terse one-line actionable comments with prefix/location/problem/Fix, and recommends safe fixes for SQL injection risk, token logging exposure, and timeout regression.",
      "assertions": [
        "Reports security findings before performance or style findings when multiple risks exist.",
        "Each finding is a single prefixed line containing location, problem, and 'Fix:'.",
        "Fix directions are concrete and minimal (e.g., parameterized query, remove/redact token logging, restore sane timeout bound)."
      ]
    }
  ]
}
