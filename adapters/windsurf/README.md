# Windsurf Adapter

## Planned install command

```bash
windsurf rules add https://github.com/sprngr/rubber-duck
```

## Fallback mapping

- `AGENTS.md` → project root instruction baseline
- `agents/*.agent.md` → routing/subagent contract references
- `skills/**` → manually copied prompts into Windsurf-compatible rules
