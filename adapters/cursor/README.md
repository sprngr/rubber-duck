# Cursor Adapter

## Planned install command

```bash
cursor rules add https://github.com/sprngr/rubber-duck
```

## Fallback mapping

- `AGENTS.md` → project root instruction baseline
- `agents/*.agent.md` → routing/subagent contract references
- `skills/**` → manually copied prompts into team rules/snippets
