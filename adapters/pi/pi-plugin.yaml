name: rubber-duck
display_name: Rubber Duck
version: 0.1.0
description: Socratic router + duckling agents + skills for Pi workflows.
repository: https://github.com/sprngr/rubber-duck
harness: pi

commands_file: commands.yaml

install:
  target_root: ${XDG_CONFIG_HOME:-$HOME/.config}/pi/rubber-duck
