{
  "name": "rubber-duck-site",
  "private": true,
  "version": "0.0.0",
  "description": "Static documentation site for rubber-duck, built with docmd.",
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build",
    "validate": "docmd validate"
  },
  "devDependencies": {
    "@docmd/core": "^0.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
