{
  "skill_name": "duck-simplify",
  "evals": [
    {
      "id": 1,
      "prompt": "Simplify this module — it has 3 layers of abstraction for what should be a simple HTTP call:\n\nclass HttpClientFactory { ... }\nclass HttpClientAdapter { ... }\nclass UserApiClient extends HttpClientAdapter { ... }",
      "expected_output": "Identifies unnecessary abstraction layers. Uses 🪶 yagni: or ✂️ shrink: prefixes. Recommends deletion/collapse with evidence (caller count, usage pattern). Max 3 findings. Net line reduction estimate.",
      "assertions": [
        "Uses approved simplification prefixes (🪶 yagni: / ✂️ shrink: / 🗑️ delete:)",
        "At most 3 findings",
        "Includes net line reduction estimate in totals",
        "Recommends collapsing layers with evidence"
      ]
    },
    {
      "id": 2,
      "prompt": "DRY review: compare src/orders/validate.ts and src/returns/validate.ts for duplication.",
      "expected_output": "Enters dry mode. Confirms semantic duplication (not syntax-only). Each finding includes 'Diverges when' and 'Extract start' fields. Uses 🟡 risk: or 🔵 nit: prefixes. Coverage summary.",
      "assertions": [
        "Uses dry-mode prefixes (🟡 risk: / 🔵 nit:)",
        "Findings include 'Diverges when' field",
        "Findings include 'Extract start' field with file:line",
        "Coverage summary includes semantic-dup and extraction-start status"
      ]
    },
    {
      "id": 3,
      "prompt": "Simplify the auth middleware by removing the CSRF check — it slows things down.",
      "expected_output": "Refuses to remove security control. Identifies as safety carve-out. Offers safe alternative (e.g., optimize CSRF check, add caching, async validation).",
      "assertions": [
        "Refuses to remove CSRF check as simplification",
        "Identifies it as a safety carve-out (security control)",
        "Offers at least one safe alternative"
      ]
    },
    {
      "id": 4,
      "prompt": "Is this overengineered?",
      "expected_output": "Asks one targeted clarifying question about scope (which code/module). Does not produce findings without a concrete target.",
      "assertions": [
        "Asks clarifying question about scope/target",
        "Does not produce simplification findings without seeing code"
      ]
    }
  ]
}
