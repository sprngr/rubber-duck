# Task-03 Fixture (Plain Files)

Purpose: reproducible design artifacts for data model redesign tradeoff evaluation.

## Contents

- `schema/current-schema.sql` — current state model
- (next batches) proposed schema sketch, migration notes, plain test oracle, prompt snippet

## Usage

1. Use these files as design context artifacts (no runtime required).
2. Pair with `experiments/task-03/packet.md` prompt.
3. Score options/tradeoffs using `experiments/task-03/scorecard.md`.
