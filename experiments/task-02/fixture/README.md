# Task-02 Fixture (Plain Files)

Purpose: reproducible API+DB debugging artifacts for data-integrity scenario.

## Contents

- `src/api/address/updateDefaultAddress.ts` — baseline buggy write path
- (next batches) repo/schema/test oracle/snippets

## Usage

1. Copy `src/` into run workspace.
2. Use packet prompt against this code artifact.
3. Use plain `tests/*.spec.txt` files (added in next batch) as scoring oracle.
